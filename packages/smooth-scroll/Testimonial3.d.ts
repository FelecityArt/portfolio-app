export interface Testimonial3Props {
  /**
   * Photo — pass as `s6Hyhi6bB` not `photo`.
   */
  s6Hyhi6bB?: string;
  /**
   * Name — pass as `ghSnNrlLB` not `name`.
   * @default "Name"
   */
  ghSnNrlLB?: string;
  /**
   * Company — pass as `K17pYsUnz` not `company`.
   * @default "Company"
   */
  K17pYsUnz?: string;
  /**
   * Review — pass as `ZWis7nx6g` not `review`.
   * @default "Review"
   */
  ZWis7nx6g?: string;
  /** Additional properties */
  [key: string]: unknown;
}
