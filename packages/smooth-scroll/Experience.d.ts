export interface ExperienceProps {
  /**
   * Position — pass as `TfTg6uI7R` not `position`.
   * @default "Position"
   */
  TfTg6uI7R?: string;
  /**
   * Company — pass as `wk9RHbxJy` not `company`.
   * @default "Company"
   */
  wk9RHbxJy?: string;
  /**
   * Dates — pass as `Bs4kC58nX` not `dates`.
   * @default "Dates"
   */
  Bs4kC58nX?: string;
  /**
   * Description — pass as `kBvR6ZVlN` not `description`.
   * @default "Description"
   */
  kBvR6ZVlN?: string;
  /** Additional properties */
  [key: string]: unknown;
}
